Para compilar o sistema deve se utilizar o Makefile incluído.

Para executar o sistema deve se executar dois programas, da seguinte forma:

./server <numero da porta>
./client <nome do host> <numero da porta>